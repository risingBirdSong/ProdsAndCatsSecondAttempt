@page
@model ProdsAndCatsSecondAttempt.Pages.Categories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categorie[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categorie[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categorie[0].UpdatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Categorie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdatedAt)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CategorieID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CategorieID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CategorieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
